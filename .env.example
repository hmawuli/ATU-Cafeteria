# Root-level reference only. Runtime environment files belong to each service.
# Flutter API endpoint can be supplied at build/run time:
# flutter run --dart-define=API_BASE_URL=http://10.0.2.2:8000/api/

BACKEND_URL=http://127.0.0.1:8000
